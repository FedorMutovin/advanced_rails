= render 'search/form'
.search_results_block
.questions
  = render @questions
p= link_to 'Ask question', new_question_path if can?(:create, Question)
.question-errors
  = render 'shared/errors', resource: @question
p= link_to 'My rewards', rewards_path if can?(:index, Reward)
